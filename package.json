{
  "name": "petstore-api-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Suite de pruebas API (Playwright) para Swagger Petstore v2: login, listar mascotas, consultar mascota, crear orden.",
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^25.0.3"
  },
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test && playwright show-report"
  }
}
